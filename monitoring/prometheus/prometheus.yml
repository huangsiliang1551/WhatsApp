global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "whatsapp_app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000

  - job_name: "whatsapp_worker"
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000
